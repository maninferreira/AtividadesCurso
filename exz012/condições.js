// Condições IF e ELSE 

var velocidade = 90

console.log(`A velocidade do seu carro e ${velocidade}km/h`)

if (velocidade > 60) { // se essa condição for atendida
    console.log (`tu utrapasou a velocidade permitida. Chegar no destino vai no DETRAN e devolve a CNH`)
} else { //se não
    console.log (`Boa viagem! Use o cinto de segurança. (para não morrer) `)
}